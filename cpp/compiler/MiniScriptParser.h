// Hand-converted MiniScript Parser from GPPG-generated C# code
// Original generated by Gardens Point Parser Generator

#pragma once
#include "ShiftReduceParserCode.h"
#include <iostream>

namespace MiniScript {

// Token enumeration
enum class Tokens {
	error = 2,
	END_OF_FILE = 3,
	NUMBER = 4,
	PLUS = 5,
	MINUS = 6,
	TIMES = 7,
	DIVIDE = 8,
	LPAREN = 9,
	RPAREN = 10
};

// Forward declaration
class Lexer;

// Parser class
class Parser : public QUT::Gppg::ShiftReduceParser<int, QUT::Gppg::LexLocation> {
private:
	static QUT::Gppg::Rule* rules[9];
	static QUT::Gppg::State* states[16];
	static String nonTerms[3];
	static bool initialized;

	static void InitializeStaticData();

protected:
	// Override abstract methods from base class
	virtual void Initialize() override;
	virtual void DoAction(int action) override;
	virtual String TerminalToString(int terminal) override;

public:
	Parser();
	virtual ~Parser();
};

} // namespace MiniScript
