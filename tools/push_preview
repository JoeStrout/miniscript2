#!/bin/bash
# Backward-compatible wrapper: push a numbered preview release.
# Everything lives in push_release; this only supplies the channel.
# Usage: tools/push_preview [number] [--force]
exec "$(dirname "$0")/push_release" preview "$@"
