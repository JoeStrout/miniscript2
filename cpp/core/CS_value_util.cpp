// CS_value_util.cpp - Conversion utilities between host (B-side) and runtime (A-side) types

#include "CS_value_util.h"

#include "layer_defs.h"
// Layer 4 is the top layer - no restrictions on dependencies

// Currently all functions are inline in the header
// This file exists for future non-inline conversion utilities
